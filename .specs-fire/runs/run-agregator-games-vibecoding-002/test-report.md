---
run: run-agregator-games-vibecoding-002
work_item: central-price-refresh
intent: unified-price-watchlist-mvp
generated: 2026-07-25T15:45:00Z
status: passed
---

# Test Report: Централизованное обновление цен

## Summary

| Category | Passed | Failed | Skipped | Coverage |
|----------|--------|--------|---------|----------|
| Integration | 19 | 0 | 0 | not collected |
| **Total** | **19** | **0** | **0** | not collected |

## Acceptance Criteria Validation

- ✅ **Каждая уникальная игра обновляется не более одного раза на источник** — job имеет ключ `game_id:source`, `ShouldBeUnique` и `WithoutOverlapping`.
- ✅ **Released обновляются каждые 3 часа** — следующий успех использует `PRICE_REFRESH_INTERVAL_HOURS=3`.
- ✅ **Announced — Steam раз в сутки, без marketplaces** — dispatcher и job блокируют Plati/GGsel, Steam получает 24h интервал.
- ✅ **После релиза включается market cycle** — тест проверяет немедленное due-состояние существующего market source.
- ✅ **Первичное обновление не блокирует HTTP** — favorite store создаёт game/state и dispatches job; тест использует Queue fake.
- ✅ **Есть защита, budgets и backoff** — named rate limiters, scheduler lock, unique job и backoff 1/5/15/30/60 минут.
- ✅ **Ошибка не удаляет успешную цену** — тест сохраняет current price и проверяет failed state/backoff.

## Tests Written

### Integration Tests

- `backend/tests/Feature/CentralPriceRefreshTest.php` — refresh persistence, announced/released gate, failure preservation, queued favorite и stored API.

## Test Commands

```powershell
$env:DB_USERNAME = 'gpa_test_runner'
$env:DB_PASSWORD = 'gpa_test_runner'
$env:DB_DATABASE = 'gpa_test'
php artisan test --filter=CentralPriceRefreshTest
php artisan test
vendor/bin/pint
```

## Coverage Details

Coverage instrumentation is not configured in this Laravel test environment.

## Issues Found

Python syntax validation could not run because the local Python launcher has no installed interpreter. PHP syntax was checked for all newly added backend files; the route was verified with `php artisan route:list --path=games`.

## Ready for Completion

- [x] All tests passing
- [x] Acceptance criteria validated
- [x] No critical issues open

---
*Generated by specs.md - FIRE Flow Run run-agregator-games-vibecoding-002*
