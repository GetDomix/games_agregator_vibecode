---
run: run-agregator-games-vibecoding-001
work_item: canonical-game-price-model
intent: unified-price-watchlist-mvp
generated: 2026-07-25T14:26:18Z
status: passed
---

# Test Report: Единая модель игр и цен

## Summary

| Category | Passed | Failed | Skipped | Coverage |
|----------|--------|--------|---------|----------|
| Unit | 4 | 0 | 0 | Not collected |
| Feature / Integration | 10 | 0 | 0 | Not collected |
| **Total** | **14** | **0** | **0** | **Not collected** |

Full suite assertions: **49**. New work-item tests: **6 tests, 28 assertions**.

## Acceptance Criteria Validation

- ✅ **Игра однозначно идентифицируется Steam `appid` и хранит метаданные релиза** — уникальность и модель проверены на PostgreSQL.
- ✅ **Текущая цена и история разделены по площадке и виду предложения** — созданы отдельные таблицы с PostgreSQL CHECK-ограничениями.
- ✅ **Несколько пользователей используют один набор объективных цен** — две записи избранного связаны с одной игрой и общей текущей ценой.
- ✅ **Клиент не может подменить каноническую текущую цену** — публичные write-endpoint для новых ценовых таблиц отсутствуют; существующий API не изменён.
- ✅ **Legacy-данные перенесены без потери избранного и дубликатов** — миграция протестирована через down, заполнение старых таблиц и повторный up.
- ✅ **Миграции проверены на чистой и заполненной PostgreSQL** — `RefreshDatabase` проверил чистый прогон, отдельный сценарий проверил legacy-backfill.

## Tests Written

### Unit Tests

Новые unit-тесты не требовались: проверяемая логика сосредоточена в схеме PostgreSQL, миграционном backfill и Eloquent-связях.

### Integration Tests

- `backend/tests/Feature/CanonicalGamePriceModelTest.php` — чистая схема и общие связи.
- `backend/tests/Feature/CanonicalGamePriceModelTest.php` — уникальность Steam `appid`.
- `backend/tests/Feature/CanonicalGamePriceModelTest.php` — уникальность текущей цены по игре, источнику и виду.
- `backend/tests/Feature/CanonicalGamePriceModelTest.php` — отдельные PostgreSQL CHECK для источника и вида предложения.
- `backend/tests/Feature/CanonicalGamePriceModelTest.php` — backfill избранного и последней Steam-цены без ложной market-истории.

## Test Commands

```powershell
$env:DB_USERNAME = 'gpa_test_runner'
$env:DB_PASSWORD = 'gpa_test_runner'
$env:DB_DATABASE = 'gpa_test'
php artisan test --filter=CanonicalGamePriceModelTest
php artisan test
```

## Coverage Details

Line coverage не собирался: в текущем PHP-окружении проекта не настроен coverage driver. Вместо формального процента выполнены профильный PostgreSQL migration test и полный регрессионный набор.

## Issues Found

| Issue | Severity | Status |
|-------|----------|--------|
| PostgreSQL первоначально не был запущен | Environment | Resolved: поднят `db` из Docker Compose. |
| PHPUnit credentials не совпадали с локальным контейнером | Environment | Resolved: создана отдельная локальная роль `gpa_test_runner` только для `gpa_test`. |

## Ready for Completion

- [x] All tests passing
- [ ] Numeric coverage target met — coverage driver отсутствует
- [x] All acceptance criteria validated
- [x] No critical issues open

---
*Generated by specs.md - fabriqa.ai FIRE Flow Run run-agregator-games-vibecoding-001*
