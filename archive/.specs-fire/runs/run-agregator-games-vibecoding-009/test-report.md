---
run: run-agregator-games-vibecoding-009
work_item: telegram-access-security-hardening
intent: unified-price-watchlist-mvp
generated: 2026-07-25T19:00:00Z
status: passed
---

# Test Report: Безопасная Telegram-привязка и доступ бота

## Summary

| Category | Passed | Failed | Skipped | Coverage |
|----------|--------|--------|---------|----------|
| Backend feature | 41 | 0 | 0 | n/a |
| Bot unit/contract | 6 | 0 | 0 | n/a |
| Frontend lint/build | 2 | 0 | 0 | n/a |
| **Total** | **49** | **0** | **0** | **n/a** |

## Acceptance Criteria Validation

- ✅ **Личный чат и identity-only resolution** — group chat получает 422, private `/start` создаёт одну identity.
- ✅ **Идемпотентный `/start`** — два запроса создают ровно один `ExternalIdentity` и одного пользователя.
- ✅ **Полная отвязка** — удаляет identity; следующий bot API запрос возвращает 401.
- ✅ **Безопасный OIDC readiness** — отсутствующая конфигурация даёт 503 с публичным detail, а не 500.
- ✅ **Безопасность исторических group-chat routes** — добавлена migration, очищающая только отрицательные Telegram chat id.
- ✅ **Контракт бота** — legacy bind передаёт Telegram user id, backend проверяет private-chat равенство.

## Tests Written

### Backend Integration Tests

- `backend/tests/Feature/TelegramReleaseRegressionTest.php` — shared group chat и unlink regression.
- `backend/tests/Feature/TelegramAccessSecurityTest.php` — повторный private start и OIDC unavailable.
- `backend/tests/Feature/TelegramBotInterfaceTest.php` — обновлён контракт личного chat id.
- `backend/tests/Feature/TelegramLinkConflictTest.php` — обновлён контракт legacy bind.

### Bot Tests

- `bot/tests/test_api_client.py` — bind передаёт Telegram user identity вместе с личным chat id.

## Test Commands

```bash
cd backend
php artisan test

cd frontend
npm run lint && npm run build

docker run --rm --mount type=bind,source=C:/Users/stati/PycharmProjects/agregator_games_vibecoding/bot,target=/app -w /app agregator_games_vibecoding-radar-bot:latest python -m unittest discover -s tests -v
```

## Notes

- Frontend lint завершился с существующим предупреждением `src/brand.tsx` о Fast Refresh; ошибок нет.
- Реальные OIDC credentials сознательно не тестировались и не добавлялись. Production readiness остаётся внешним действием: после deploy должны быть заданы три server-side переменные.
- Регрессии Pro/квот сохранены в QA-отчёте и сознательно не входят в этот security work item; они будут реализованы в `free-search-monetization-cleanup`.

## Ready for Completion

- [x] All tests passing
- [x] Acceptance criteria validated
- [x] No critical security issue from this work item remains open locally

---
*Generated by specs.md - FIRE Flow Run run-agregator-games-vibecoding-009*
