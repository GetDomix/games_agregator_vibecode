# Temporary HTTP edge for IP-based VPS checks. Production uses Caddyfile.production via the strict overlay.
:80 {
	encode zstd gzip
	reverse_proxy frontend:80 {
		header_up X-Real-IP {remote_host}
		header_up X-Forwarded-Proto http
	}
}
